# Resistant-class-count distribution (P(C=c)) for fully-observed panels, faceted by hospital (panel size shown in the facet label) so C=4 for a 4-class panel is never plotted alongside C=5 for a 5-class panel as if they were the same quantity.

Resistant-class-count distribution (P(C=c)) for fully-observed panels,
faceted by hospital (panel size shown in the facet label) so C=4 for a
4-class panel is never plotted alongside C=5 for a 5-class panel as if
they were the same quantity.

## Usage

``` r
.ppc_plot_resistant_count_distribution(st, title_base)
```
