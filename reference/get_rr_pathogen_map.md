# Get RR Pathogen Mapping

Returns a mapping from normalized organism names to RR pathogen
categories used in burden estimation. Reads from
inst/extdata/organisms.csv.

## Usage

``` r
get_rr_pathogen_map()
```

## Value

Data frame with columns organism_name and rr_pathogen
