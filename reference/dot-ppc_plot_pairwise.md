# Pairwise co-resistance PPC. Returns a NAMED LIST of four plots (RR, RS, SR, SS) instead of one page – the previous single forest plot mixed all four cell types with hospital x class-pair labels on one axis, which was the single worst page in the report. Each returned plot facets by class pair (short labels) with hospital on the y-axis.

Pairwise co-resistance PPC. Returns a NAMED LIST of four plots (RR, RS,
SR, SS) instead of one page – the previous single forest plot mixed all
four cell types with hospital x class-pair labels on one axis, which was
the single worst page in the report. Each returned plot facets by class
pair (short labels) with hospital on the y-axis.

## Usage

``` r
.ppc_plot_pairwise(st, title_base)
```
