# Check panel eligibility thresholds per hospital x class cell

Check panel eligibility thresholds per hospital x class cell

## Usage

``` r
.validate_panel_eligibility(
  event_class_data,
  class_cols,
  upper_re_col,
  pathogen_col,
  min_tested = 30L,
  min_resistant = 5L,
  min_susceptible = 5L
)
```
