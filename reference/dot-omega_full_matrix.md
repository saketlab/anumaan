# Mirror an upper-triangle class_1/class_2 correlation table into a full symmetric matrix (both (i,j) and (j,i), plus a diagonal of 1s/NA), for a complete-looking heatmap instead of a half-populated triangle.

Mirror an upper-triangle class_1/class_2 correlation table into a full
symmetric matrix (both (i,j) and (j,i), plus a diagonal of 1s/NA), for a
complete-looking heatmap instead of a half-populated triangle.

## Usage

``` r
.omega_full_matrix(corr_summary, classes, diagonal_value)
```
