# Validate AMR Configuration

Checks that configuration parameters are valid.

## Usage

``` r
validate_config(config)
```

## Arguments

- config:

  An amr_config object

## Value

Logical. TRUE if valid, stops with error if invalid.
