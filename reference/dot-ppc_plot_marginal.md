# Marginal resistance PPC, faceted by class instead of one hospital x class forest plot – keeps the same statistic (marginal_resistance, hospital-level rows only; the pooled class-level rows are a separate, coarser stratum not shown here) and observed/replicated convention (red cross = observed, blue point + 95% interval = replicated).

Marginal resistance PPC, faceted by class instead of one hospital x
class forest plot – keeps the same statistic (marginal_resistance,
hospital-level rows only; the pooled class-level rows are a separate,
coarser stratum not shown here) and observed/replicated convention (red
cross = observed, blue point + 95% interval = replicated).

## Usage

``` r
.ppc_plot_marginal(st, title_base)
```
