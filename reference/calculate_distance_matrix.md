# Calculate Distance Matrix Between Locations

Computes pairwise distances between facilities or districts.

## Usage

``` r
calculate_distance_matrix(spatial_obj, units = "km")
```

## Arguments

- spatial_obj:

  sf object with spatial geometries

- units:

  Character. "km" or "m". Default "km".

## Value

Distance matrix
