% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_derivation.R
\name{prep_derive_los_from_dates}
\alias{prep_derive_los_from_dates}
\title{Derive Length of Stay from Date Columns}
\usage{
prep_derive_los_from_dates(
  data,
  admission_col = "admission_date",
  outcome_col = "outcome_date",
  los_col = "los_days",
  unit_admission_col = "unit_admission_date",
  unit_duration_col = "unit_duration_days",
  overwrite = FALSE
)
}
\arguments{
\item{data}{Data frame.}

\item{admission_col}{Character. Admission date column. Default "admission_date".}

\item{outcome_col}{Character. Outcome/discharge date column. Default "outcome_date".}

\item{los_col}{Character. Target LOS column to populate. Default "los_days".}

\item{unit_admission_col}{Character. AIIMS unit admission date fallback.
Default "unit_admission_date".}

\item{unit_duration_col}{Character. AIIMS unit duration fallback.
Default "unit_duration_days".}

\item{overwrite}{Logical. Recalculate even when \code{los_col} is already present.
Default FALSE.}
}
\value{
Data frame with \code{los_col}, \code{los_method}, and
  \code{los_confidence} populated where possible.
}
\description{
Computes LOS when missing (or always when \code{overwrite = TRUE}).
Adds \code{los_method} and \code{los_confidence} audit columns.
For AIIMS data, uses \code{unit_admission_date} + \code{unit_duration_days}
as a fallback when primary date columns are absent.
}
\details{
Priority order:
\enumerate{
  \item \code{outcome_col} - \code{admission_col}
  \item \code{unit_admission_date} + \code{unit_duration_days} (AIIMS fallback)
  \item Already-present \code{los_col} value retained as-is
}
}
