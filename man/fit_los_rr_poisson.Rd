% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden.R
\name{fit_los_rr_poisson}
\alias{fit_los_rr_poisson}
\title{Estimate Per-Class LOS Relative Risk via Quasi-Poisson Regression}
\usage{
fit_los_rr_poisson(
  data,
  patient_id_col = "PatientInformation_id",
  isolate_id_col = "isolate_id",
  facility_col = "center_name",
  organism_col = "organism_name",
  syndrome_col = "syndrome",
  infection_type_col = "type_of_infection",
  antibiotic_class_col = "antibiotic_class",
  antibiotic_name_col = "antibiotic_name",
  antibiotic_value_col = "antibiotic_value",
  date_admission_col = "date_of_admission",
  date_discharge_col = "final_outcome_date",
  date_culture_col = "date_of_first_positive_culture",
  final_outcome_col = "final_outcome",
  final_outcome_value = "Discharged",
  syndrome_name = NULL,
  organism_name = NULL,
  hai_threshold_hours = 48,
  centre_model = c("pooled_fe", "per_centre"),
  pool_method = c("weighted_mean", "median"),
  max_los = 365,
  min_n = 10L
)
}
\arguments{
\item{data}{Data frame. Merged AMR dataset at isolate x antibiotic level.}

\item{patient_id_col}{Character. Default \code{"PatientInformation_id"}.}

\item{isolate_id_col}{Character. Default \code{"isolate_id"}.}

\item{facility_col}{Character. Default \code{"center_name"}.}

\item{organism_col}{Character. Default \code{"organism_name"}.}

\item{syndrome_col}{Character. Default \code{"syndrome"}.}

\item{infection_type_col}{Character. Default \code{"type_of_infection"}.}

\item{antibiotic_class_col}{Character. Default \code{"antibiotic_class"}.}

\item{antibiotic_name_col}{Character. Default \code{"antibiotic_name"}.}

\item{antibiotic_value_col}{Character. Default \code{"antibiotic_value"}.}

\item{date_admission_col}{Character. Default \code{"date_of_admission"}.}

\item{date_discharge_col}{Character. Default \code{"final_outcome_date"}.}

\item{date_culture_col}{Character. Default
\code{"date_of_first_positive_culture"}.}

\item{final_outcome_col}{Character. Default \code{"final_outcome"}.}

\item{final_outcome_value}{Character. Default \code{"Discharged"}.}

\item{syndrome_name}{Character or NULL. Filter to syndrome (e.g. "BSI").
NULL = all syndromes (universal RR).}

\item{organism_name}{Character or NULL. Pathogen(s) to process. NULL = all.}

\item{hai_threshold_hours}{Numeric. HAI/CAI gap threshold. Default \code{48}.}

\item{centre_model}{Character. \code{"pooled_fe"} (default) or
\code{"per_centre"}.}

\item{pool_method}{Character. Used when centre_model = "per_centre".
\code{"weighted_mean"} (default) or \code{"median"}.}

\item{max_los}{Numeric. Default \code{200}.}

\item{min_n}{Integer. Minimum patients per model. Default \code{10}.}
}
\value{
Data frame: pathogen, antibiotic_class, RR_LOS, CI_lower, CI_upper,
  n_patients, centre_model, syndrome_scope. When centre_model = "per_centre",
  attribute "per_centre_rr" contains per-centre estimates.
}
\description{
For each pathogen k and antibiotic class c, fits:
  log(E[LOS_i]) = b0 + b_c * class_ci + b_HAI * HAI_i + sum_f gf * I(centre=f)
and returns RR_kc_LOS = exp(b_c) with 95% Wald CI.
}
\details{
LOS is computed with HAI/CAI-specific clock (see compute_patient_los).
Resistance is classified at antibiotic class level (class = 1 if any drug
in class = R). HAI enters as a binary covariate to absorb residual baseline
severity differences beyond the LOS clock adjustment. No PreDays covariate
is used — the LOS clock change from admission to culture date for HAI
patients already removes the pre-infection period from the outcome.

Stated limitation: when syndrome_name is supplied, RR_kc_LOS is
syndrome-specific. Downstream PAF applies this RR to all profiles of
pathogen k, assuming syndrome-invariant LOS prolongation across infection
sources. Set syndrome_name = NULL for a universal RR.
}
