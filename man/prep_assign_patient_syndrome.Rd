% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_diagnosis.R
\name{prep_assign_patient_syndrome}
\alias{prep_assign_patient_syndrome}
\title{Assign One Syndrome Per Patient or Event}
\usage{
prep_assign_patient_syndrome(
  data,
  patient_col = "patient_id",
  syndrome_col = "syndrome",
  score_col = "icd_score",
  hierarchy_ref = NULL,
  hierarchy_syndrome_col = "infectious_syndrome",
  hierarchy_rank_col = "rank",
  keep_all_candidates = TRUE,
  collapse_unspecified_respiratory = FALSE,
  keep_only_burden_syndromes = FALSE
)
}
\arguments{
\item{data}{Data frame with one row per syndrome candidate.}

\item{patient_col}{Character. Patient or event ID column. Default \code{"patient_id"}.}

\item{syndrome_col}{Character. Syndrome column. Default \code{"syndrome"}.}

\item{score_col}{Character or NULL. ICD match score column for secondary priority.
Default \code{"icd_score"}.}

\item{hierarchy_ref}{Data frame or NULL. Syndrome hierarchy (from
\code{load_syndrome_hierarchy()}). If NULL, loads automatically.}

\item{hierarchy_syndrome_col}{Character. Syndrome column in hierarchy. Default \code{"infectious_syndrome"}.}

\item{hierarchy_rank_col}{Character. Rank column in hierarchy. Default \code{"rank"}.}

\item{keep_all_candidates}{Logical. TRUE → return all rows with \code{syndrome_selected}
flag; FALSE → return one row per \code{patient_col} (selected only). Default TRUE.}

\item{collapse_unspecified_respiratory}{Logical. Map "Other unspecified respiratory
site infections" → "Lower respiratory infections" before selection. Default FALSE.}

\item{keep_only_burden_syndromes}{Logical. Retain only syndromes where
\code{contributes_to_amr_burden == TRUE}. Default FALSE.}
}
\value{
Data frame. When \code{keep_all_candidates = FALSE}, includes
  \code{contributes_to_amr_burden} column from the hierarchy.
}
\description{
Selects the highest-priority syndrome per patient using the infectious
syndrome hierarchy. Matching is case-insensitive (uses normalized names).
}
\details{
Priority order per patient:
\enumerate{
  \item Hierarchy rank (lower = higher priority)
  \item ICD match score (higher = higher priority), when \code{score_col} present
  \item Alphabetical tie-break
}

This function absorbs the logic of the former \code{infer_patient_syndrome_long()}:
use \code{collapse_unspecified_respiratory = TRUE} and
\code{keep_only_burden_syndromes = TRUE} to replicate that behaviour.
}
