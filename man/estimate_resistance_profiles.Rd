% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/daly_resistance_profiles.R
\name{estimate_resistance_profiles}
\alias{estimate_resistance_profiles}
\title{Estimate Resistance Profiles: Pathway 1 (Convex) or Pathway 2 (Bayesian)}
\usage{
estimate_resistance_profiles(
  data,
  method = c("convex", "bayesian"),
  pairwise = NULL,
  panel_map = NULL,
  class_cols = NULL,
  fixed_effects = NULL,
  random_effects = NULL,
  pathogen = NULL,
  pathogen_col = "pathogen",
  eligible_pairs = NULL,
  outcome_col = NULL,
  nonfatal_values = c("Discharged", "Survived", "Alive", "discharged", "survived",
    "alive"),
  panel_eligibility = list(),
  residual_structure = c("identity", "correlated"),
  estimand = "observed_stewardship_event_mix",
  prior_config = list(),
  sampler_config = list(),
  n_posterior_draws_for_profiles = 2000L
)
}
\arguments{
\item{data}{For Pathway 1: marginals tibble from
\code{compute_marginals_from_data()}. For Pathway 2: wide-format
event-level tibble (one row per event; class columns 0/1/NA).}

\item{method}{Character. \code{"convex"} or \code{"bayesian"}.}

\item{pairwise}{Tibble or \code{NULL}. Pathway 1 only.}

\item{panel_map}{Named list or \code{NULL}. Pathway 1 only.}

\item{class_cols}{Character vector. Pathway 2 only. Required.}

\item{fixed_effects}{Character vector. Pathway 2 only. Required.}

\item{random_effects}{Character vector (1-3 elements). Pathway 2 only.
Required. Elements: hospital; [+patient]; [+admission].}

\item{pathogen}{Character or \code{NULL}. Pathway 2 only. When supplied,
filters data to a single pathogen before fitting.}

\item{pathogen_col}{Character. Default \code{"pathogen"}.}

\item{eligible_pairs}{Tibble or \code{NULL}. Pathway 2 only.}

\item{outcome_col}{Character or \code{NULL}. Pathway 2 only. Patient
outcome column for R_ALL / R_NF split.}

\item{nonfatal_values}{Character vector. Outcome values for R_NF cohort.}

\item{panel_eligibility}{Named list. Eligibility thresholds; passed to
\code{fit_bayesian_multivariate_probit()}.}

\item{residual_structure}{Character. Pathway 2. \code{"identity"} (default)
or \code{"correlated"}. See \code{fit_bayesian_multivariate_probit()}.}

\item{estimand}{Character. Estimand label. Default
\code{"observed_stewardship_event_mix"}.}

\item{prior_config}{Named list. Pathway 2 only. Priors.}

\item{sampler_config}{Named list. Pathway 2 only. Structured sampler
settings: \code{chains} (4), \code{iter_warmup} (1000),
\code{iter_sampling} (1000), \code{seed} (123), \code{adapt_delta},
\code{max_treedepth}, \code{parallel_chains}.}

\item{n_posterior_draws_for_profiles}{Integer. Pathway 2. Number of posterior
draws used for MVN simulation of profiles. Default \code{2000L}.}
}
\value{
Named list: \code{profiles}, \code{eligibility}, \code{diagnostics},
  \code{fitted_models}, \code{config_used}.
}
\description{
Top-level dispatcher. Runs either the convex optimisation pathway
(Pathway 1, aggregate surveillance data) or the Bayesian hierarchical
multivariate probit pathway (Pathway 2, facility-level AST data).
}
\details{
For Pathway 2, pass \code{pathogen} to fit one pathogen at a time -- the
recommended workflow. Run this function once per pathogen and collect the
results in the analysis repository.
}
