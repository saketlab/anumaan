% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_eda_plots.R
\name{plot_los_ridge}
\alias{plot_los_ridge}
\title{Ridge / Density Plot of Length of Stay (LOS)}
\usage{
plot_los_ridge(
  data,
  mode = c("all", "single"),
  center = NULL,
  patient_col = "patient_id",
  center_col = "center_name",
  admission_col = "admission_date",
  discharge_col = "final_outcome_date",
  filter_outcome = NULL,
  outcome_col = "final_outcome",
  min_los = 1,
  max_los = 365,
  colours = NULL,
  scale = 1.2,
  alpha = 0.7,
  base_size = 14,
  title = NULL,
  syndrome_col = NULL,
  syndrome_name = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{mode}{Character. One of \code{"all"} (ridge plot, all centres stacked
on y-axis, ordered by median LOS ascending) or \code{"single"} (density
curve for one centre specified by \code{center}).}

\item{center}{Character. Centre name required when \code{mode = "single"}.}

\item{patient_col}{Character. Column of patient identifiers.}

\item{center_col}{Character. Column of centre identifiers.}

\item{admission_col}{Character. Column containing admission date/datetime.
Default \code{"admission_date"}.}

\item{discharge_col}{Character. Column containing discharge / final outcome
date. Default \code{"final_outcome_date"}.}

\item{filter_outcome}{Character or \code{NULL}. If not \code{NULL}, only
rows whose outcome column (\code{outcome_col}) equals this value are used
(e.g. \code{"Discharged"}). Set \code{NULL} to include all outcomes.
Default \code{NULL}.}

\item{outcome_col}{Character. Column of patient outcomes, used only when
\code{filter_outcome} is not \code{NULL}. Default \code{"final_outcome"}.}

\item{min_los}{Numeric. Minimum LOS (days) to include. Rows below this
are removed (prevents \code{log(0)} issues). Default \code{1}.}

\item{max_los}{Numeric. Maximum LOS (days) to include. Default \code{365}.}

\item{colours}{Named character vector of colours for centres. If \code{NULL}
(default), the \pkg{RColorBrewer} "Set2" palette is used.}

\item{scale}{Numeric. \code{scale} argument passed to
\code{ggridges::geom_density_ridges()}. Controls overlap between ridges.
Default \code{1.2}.}

\item{alpha}{Numeric. Fill transparency (0-1). Default \code{0.7}.}

\item{base_size}{Numeric. Base font size. Default \code{14}.}

\item{title}{Character or \code{NULL}. Plot title. Auto-generated if
\code{NULL}.}

\item{syndrome_col}{Character or \code{NULL}. Column of syndrome labels for
pre-filtering. Default \code{NULL}.}

\item{syndrome_name}{Character or \code{NULL}. Syndrome value to retain.
Requires \code{syndrome_col}. Default \code{NULL}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualises the distribution of length of stay (LOS) on a log10 x-axis using
either a stacked ridge plot (\code{mode = "all"}) or a single-centre density
curve (\code{mode = "single"}).
}
\details{
LOS is computed as
\code{difftime(discharge_col, admission_col, units = "days")}.
One row per unique patient x centre x admission_date x discharge_date is
retained before plotting. Rows with \code{LOS < min_los} or
\code{LOS > max_los} are dropped.
}
