% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_daly_plots.R
\name{plot_yll_heatmap}
\alias{plot_yll_heatmap}
\title{Heatmap of YLL per 1 000 Admissions by Resistance Class and Pathogen Group}
\usage{
plot_yll_heatmap(
  data,
  type = c("associated", "attributable"),
  value_col,
  n_admissions = NULL,
  pathogen_col = "pathogen",
  class_col = "profile",
  group_col = NULL,
  syndrome_col = NULL,
  syndrome_name = NULL,
  show_values = TRUE,
  value_digits = 2,
  palette = "Spectral",
  base_size = 14,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{data}{Data frame. Must contain the columns named by
\code{pathogen_col}, \code{class_col}, and \code{value_col}.}

\item{type}{Character. \code{"associated"} (default) or
\code{"attributable"}. Used only for axis and title labels.}

\item{value_col}{Character. Required. Column containing the YLL value to
plot (e.g. \code{"YLL_class"} after distributing per-pathogen YLL across
resistance classes, or \code{"YLL_attributable_k"} if your data already
has one row per pathogen x class).}

\item{n_admissions}{Numeric or \code{NULL}. Total admissions used as the
denominator for per-1 000 normalisation. If \code{NULL} (default),
values are plotted as supplied (assumed pre-normalised).}

\item{pathogen_col}{Character. Column containing pathogen names.
Default \code{"pathogen"}.}

\item{class_col}{Character. Column containing the resistance class /
dominant profile. Default \code{"profile"}.}

\item{group_col}{Character or \code{NULL}. Column containing organism
group labels (e.g. \code{"org_group"}). If \code{NULL} (default) or not
present in \code{data}, pathogen names are used on the y-axis.}

\item{syndrome_col}{Character or \code{NULL}. Syndrome filter column.
Default \code{NULL}.}

\item{syndrome_name}{Character or \code{NULL}. Syndrome value to retain.
Requires \code{syndrome_col}. Default \code{NULL}.}

\item{show_values}{Logical. Print rounded values inside each cell.
Default \code{TRUE}.}

\item{value_digits}{Integer. Decimal places for cell labels. Default 2.}

\item{palette}{Character. RColorBrewer palette for the fill scale.
Default \code{"Spectral"}.}

\item{base_size}{Numeric. Base font size. Default 14.}

\item{title}{Character. Custom plot title. Auto-generated if
\code{NULL}.}

\item{subtitle}{Character. Custom subtitle. Auto-generated if
\code{NULL}.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a tile heatmap where the x-axis shows resistance classes (dominant
AMR profiles), the y-axis shows pathogen groups (or individual pathogens),
and each cell's fill reflects the associated or attributable YLL burden per
1 000 admissions.
}
\details{
The input \code{data} should be the \code{$by_class} slot (for associated)
or the \code{$by_profile} slot (for attributable) returned by
\code{daly_calc_yll_associated()} / \code{daly_calc_yll_attributable()}.
Both slots contain columns \code{pathogen}, \code{profile}, and
\code{YLL_Kdelta}.

If an organism-group column has been pre-joined onto \code{data} (e.g.
\code{org_group}), supply its name via \code{group_col} and the function
will aggregate to the group x class level. When \code{group_col} is
\code{NULL} or absent, individual pathogen names are used on the y-axis.
}
