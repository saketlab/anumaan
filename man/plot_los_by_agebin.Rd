% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_eda_plots.R
\name{plot_los_by_agebin}
\alias{plot_los_by_agebin}
\title{Boxplot of Length of Stay by Age Group}
\usage{
plot_los_by_agebin(
  data,
  mode = c("faceted", "overall", "single"),
  center = NULL,
  patient_col = "PatientInformation_id",
  agebin_col = "Age_bin",
  age_col = "age_years",
  age_breaks = NULL,
  age_labels = NULL,
  center_col = "center_name",
  admission_col = "admission_date",
  discharge_col = "final_outcome_date",
  min_los = 1,
  max_los = 365,
  age_levels = NULL,
  fill_colour = "#2C7FB8",
  fill_alpha = 0.7,
  outlier_alpha = 0.25,
  box_width = 0.6,
  ncol = 3,
  base_size = 14,
  title = NULL,
  subtitle = NULL,
  syndrome_col = NULL,
  syndrome_name = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{mode}{Character. One of \code{"faceted"}, \code{"overall"}, or
\code{"single"}.}

\item{center}{Character. Centre name required when \code{mode = "single"}.}

\item{patient_col}{Character. Column of patient identifiers.}

\item{agebin_col}{Character. Pre-existing age group column. Used when
\code{age_breaks} is \code{NULL}. Default \code{"Age_bin"}.}

\item{age_col}{Character. Continuous age column used when \code{age_breaks}
is provided. Default \code{"age_years"}.}

\item{age_breaks}{Numeric vector or \code{NULL}. Breakpoints for deriving
custom bins from \code{age_col} (e.g. \code{c(0, 5, 15, 30, 50, 70, Inf)}).
\code{NULL} = use \code{agebin_col} directly.}

\item{age_labels}{Character vector or \code{NULL}. Labels for each bin
created by \code{age_breaks}. Must have length
\code{length(age_breaks) - 1}. \code{NULL} = auto-generate from breaks.}

\item{center_col}{Character. Column of centre identifiers.}

\item{admission_col}{Character. Admission date column. Default
\code{"admission_date"}.}

\item{discharge_col}{Character. Discharge / final outcome date column.
Default \code{"final_outcome_date"}.}

\item{min_los}{Numeric. Minimum LOS (days) to include. Default \code{1}.}

\item{max_los}{Numeric. Maximum LOS (days) to include. Default \code{365}.}

\item{age_levels}{Character vector or \code{NULL}. Custom bin order when
using \code{agebin_col}. \code{NULL} = auto-sort.}

\item{fill_colour}{Character. Box fill colour. Default \code{"#2C7FB8"}.}

\item{fill_alpha}{Numeric. Box fill transparency (0-1). Default \code{0.7}.}

\item{outlier_alpha}{Numeric. Outlier point transparency (0-1). Default
\code{0.25}.}

\item{box_width}{Numeric. Width of each box. Default \code{0.6}.}

\item{ncol}{Integer. Number of facet columns (\code{mode = "faceted"} only).
Default \code{3}.}

\item{base_size}{Numeric. Base font size. Default \code{14}.}

\item{title}{Character or \code{NULL}. Plot title. Auto-generated if
\code{NULL}.}

\item{subtitle}{Character or \code{NULL}. Plot subtitle. \code{NULL} = none.}

\item{syndrome_col}{Character or \code{NULL}. Syndrome column for
pre-filtering. Default \code{NULL}.}

\item{syndrome_name}{Character or \code{NULL}. Syndrome value to retain.
Requires \code{syndrome_col}. Default \code{NULL}.}
}
\value{
A \code{ggplot} object.
}
\description{
Displays the distribution of LOS (y-axis, log10 scale) across patient age
groups (x-axis) using boxplots. Supports \code{"faceted"} (one panel per
centre), \code{"overall"} (all centres pooled), and \code{"single"}
(one centre) modes.
}
\details{
LOS is computed as \code{difftime(discharge_col, admission_col, units = "days")}.
One row per unique patient x centre x admission x discharge is retained.
Rows with \code{LOS < min_los} or \code{LOS > max_los} are dropped.

\strong{Two ways to supply age groups:}
\enumerate{
  \item \strong{Pre-existing column} (default) -- set \code{agebin_col} to an
    existing categorical column (e.g. \code{"Age_bin"}). Bins are sorted
    automatically (\code{<1}, \code{1-5}, ..., \code{85+}); override order
    with \code{age_levels}.
  \item \strong{Custom breaks from continuous age} -- set \code{age_breaks}
    to a numeric vector (e.g. \code{c(0, 5, 15, 30, 50, 70, Inf)}) and
    \code{age_col} to the continuous age column (default \code{"age_years"}).
    Bins are derived via \code{cut()} and labelled automatically.
    \code{agebin_col} is ignored when \code{age_breaks} is provided.
}
}
