% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_events.R
\name{prep_create_event_ids}
\alias{prep_create_event_ids}
\title{Create Event IDs from Patient-Level Data}
\usage{
prep_create_event_ids(
  data,
  patient_col = "patient_id",
  date_col = "date_of_culture",
  organism_col = "organism_normalized",
  specimen_col = "specimen_type",
  antibiotic_col = "antibiotic_name",
  value_col = "antibiotic_value",
  gap_days = 14,
  culture_col = "id_organisminfo",
  admission_col = "date_of_admission",
  keep_event_culture = c("all", "closest_to_admission")
)
}
\arguments{
\item{data}{Data frame (long format -- one row per antibiotic test).}

\item{patient_col}{Patient ID column. Default "patient_id".}

\item{date_col}{Culture date column. Default "date_of_culture".}

\item{organism_col}{Organism column. Default "organism_normalized".}

\item{specimen_col}{Specimen type column. Default "specimen_type".}

\item{antibiotic_col}{Antibiotic name column. Default "antibiotic_name".}

\item{value_col}{Susceptibility result column (S/I/R). Default "antibiotic_value".}

\item{gap_days}{Days threshold: gap > gap_days triggers a new event. Default 14.}

\item{culture_col}{Culture/isolate identifier column. When present, same-day
cultures from the same site and organism are evaluated separately before
duplicate culture removal. Default \code{"id_organisminfo"}.}

\item{admission_col}{Admission date column. Used when \code{keep_event_culture}
is \code{"closest_to_admission"}. Default \code{"date_of_admission"}.}

\item{keep_event_culture}{Character. \code{"all"} keeps all culture rows after
assigning event IDs. \code{"closest_to_admission"} keeps all antibiotic rows
from only the culture date closest to admission within each event/organism/site.
Default \code{"all"}.}
}
\value{
Original data frame with \code{event_id} column added.
}
\description{
Converts patient-level isolate data to event-level data by grouping repeat
cultures from the same infection episode.
}
\details{
Event classification rules (in order of precedence):

| Scenario                                            | Result          |
|-----------------------------------------------------|-----------------|
| Different body sites (any date)                     | Separate events |
| Same site, same day, same organism, same ABG        | One event       |
| Same site, same day, same organism, diff ABG        | Separate events |
| Same site, same organism, within gap_days, same ABG | Same event      |
| Same site, same organism, within gap_days, ABG changed | New event    |
| Same site, same organism, after > gap_days          | New event       |
| Same site, same day, different organisms            | Separate events |

Event IDs are numbered GLOBALLY per patient in chronological order.
}
