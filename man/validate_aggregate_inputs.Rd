% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile_convex.R
\name{validate_aggregate_inputs}
\alias{validate_aggregate_inputs}
\title{Validate Pre-computed Aggregate Marginal Inputs}
\usage{
validate_aggregate_inputs(
  marginals,
  pairwise = NULL,
  pathogen_col = "pathogen",
  class_col = "antibiotic_class",
  rate_col = "marginal_resistance",
  n_tested_col = "n_tested",
  n_resistant_col = "n_resistant",
  class1_col = "antibiotic_class_1",
  class2_col = "antibiotic_class_2",
  pairwise_rate_col = "pairwise_resistance_prevalence",
  min_classes_per_pathogen = 2L
)
}
\arguments{
\item{marginals}{Data frame with at minimum columns \code{pathogen_col},
\code{class_col}, and \code{rate_col}. Optionally also \code{n_tested_col}
and \code{n_resistant_col}.}

\item{pairwise}{Data frame or \code{NULL}. Pairwise co-resistance table with
columns \code{pathogen_col}, \code{class1_col}, \code{class2_col}, and
\code{pairwise_rate_col}. Default \code{NULL}.}

\item{pathogen_col}{Character. Default \code{"pathogen"}.}

\item{class_col}{Character. Default \code{"antibiotic_class"}.}

\item{rate_col}{Character. Marginal resistance rate column.
Default \code{"marginal_resistance"}.}

\item{n_tested_col}{Character or \code{NULL}. Default \code{"n_tested"}.}

\item{n_resistant_col}{Character or \code{NULL}. Default \code{"n_resistant"}.}

\item{class1_col}{Character. Default \code{"antibiotic_class_1"}.}

\item{class2_col}{Character. Default \code{"antibiotic_class_2"}.}

\item{pairwise_rate_col}{Character. Default \code{"pairwise_resistance_prevalence"}.}

\item{min_classes_per_pathogen}{Integer. Pathogens with fewer classes are
flagged. Default \code{2L}.}
}
\value{
Tibble of check results (same schema as
  \code{validate_profile_inputs()}). Stops on any \code{"fail"}.
}
\description{
Validates a pre-computed marginal resistance table (e.g. from GBD or a
surveillance network) before it is passed to \code{estimate_profiles_convex()}.
Checks prevalence bounds, duplicate rows, zero-denominator cells, impossible
pairwise values, and minimum class counts per pathogen.
}
