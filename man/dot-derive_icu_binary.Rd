% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden.R, R/yll.R
\name{.derive_icu_binary}
\alias{.derive_icu_binary}
\title{Derive ICU Binary Flag per Patient}
\usage{
.derive_icu_binary(
  data,
  patient_id_col = "PatientInformation_id",
  unit_type_col = "unit_type",
  icu_values = c("ICU", "Intensive Care", "Critical Care", "PICU", "NICU"),
  missing_threshold = 0.1
)

.derive_icu_binary(
  data,
  patient_id_col = "PatientInformation_id",
  unit_type_col = "unit_type",
  icu_values = c("ICU", "Intensive Care", "Critical Care", "PICU", "NICU"),
  missing_threshold = 0.1
)
}
\arguments{
\item{data}{Data frame at drug-test level.}

\item{patient_id_col}{Character. Default \code{"PatientInformation_id"}.}

\item{unit_type_col}{Character. Default \code{"unit_type"}.}

\item{icu_values}{Character vector. ICU location labels.}

\item{missing_threshold}{Numeric. Proportion above which a 3-level factor
is returned. Default \code{0.10}.}
}
\value{
Patient-level data frame: \code{patient_id_col} and \code{ICU}
  (integer 0/1, or factor \code{"ICU"/"Ward"/"Unknown"} when missing is
  high).

Patient-level data frame with \code{patient_id_col} and \code{ICU}.
}
\description{
Collapses unit-type data (one row per drug test per patient) to a
patient-level binary ICU indicator using the \strong{"ever in ICU"} rule:
if a patient has \emph{any} row with an ICU-type location during that
admission, \code{ICU = 1}; otherwise \code{ICU = 0}.

Collapses unit-type data to a patient-level binary ICU indicator using the
"ever in ICU" rule. Returns integer 0/1 or a 3-level factor when missing
data is high.
}
\details{
Missing unit-type values are coded as \code{NA}. When the proportion of
patients with \emph{all} unit-type rows missing exceeds
\code{missing_threshold}, the function returns a three-level ordered factor
(\code{"ICU"} / \code{"Ward"} / \code{"Unknown"}) and emits a message
recommending that the \code{"Unknown"} level be included in the model.
}
\keyword{internal}
