% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_schema.R
\name{prep_standardize_column_names}
\alias{prep_standardize_column_names}
\title{Standardize Column Names to Package Convention}
\usage{
prep_standardize_column_names(
  data,
  mapping = default_column_mappings,
  fuzzy_match = TRUE,
  fuzzy_threshold = 0.3,
  interactive = FALSE
)
}
\arguments{
\item{data}{A data frame with raw column names.}

\item{mapping}{Named list of standard_name -> aliases vectors. Default uses
\code{default_column_mappings}.}

\item{fuzzy_match}{Logical. Enable fuzzy matching for unresolved standards.
Default TRUE.}

\item{fuzzy_threshold}{Numeric. Max Jaro-Winkler distance (0-1). Default 0.3.}

\item{interactive}{Logical. Prompt user to confirm fuzzy matches. Default FALSE.}
}
\value{
List: \code{data} (renamed), \code{mapping_log}, \code{unmapped}.
}
\description{
Convenience wrapper around \code{prep_build_column_map()} +
\code{prep_apply_column_map()}. Builds an exact-match map from
\code{mapping} aliases, optionally extends it with fuzzy matching, then
applies the rename in one call.
}
\details{
For more control (e.g. dataset-specific maps, post-rename assertions) use
the staged API directly:
\code{prep_build_column_map()} -> \code{prep_apply_column_map()} ->
\code{prep_assert_standard_names()}.
}
