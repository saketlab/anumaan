% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_outputs.R
\name{prep_attrition_flow}
\alias{prep_attrition_flow}
\title{Track Attrition Through the Pipeline}
\usage{
prep_attrition_flow(
  flow,
  data,
  stage_name,
  reason = "",
  patient_col = "patient_id",
  event_col = "event_id"
)
}
\arguments{
\item{flow}{Data frame of previous attrition stages (or NULL to start).}

\item{data}{Current data frame at this stage.}

\item{stage_name}{Character. Short label for this stage.}

\item{reason}{Character. Description of what changed at this stage.}

\item{patient_col}{Character. Patient ID column. Default "patient_id".}

\item{event_col}{Character. Event ID column (optional). Default "event_id".}
}
\value{
Updated attrition flow data frame.
}
\description{
Records a stage entry in an attrition flow table. Call once per pipeline
stage to build up a cumulative record of how many rows and patients are
retained after each step.
}
\details{
Usage pattern:
\preformatted{
  flow <- NULL
  flow <- prep_attrition_flow(flow, data_raw,    "raw_intake",    "All raw records")
  flow <- prep_attrition_flow(flow, data_dated,  "dates_parsed",  "After date coercion")
  flow <- prep_attrition_flow(flow, data_ready,  "analysis_ready","After all filters")
  print(flow)
}
}
