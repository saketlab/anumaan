% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_eda_plots.R
\name{plot_infection_type_by_location}
\alias{plot_infection_type_by_location}
\title{Plot Infection Type (HAI/CAI) by Location (ICU/Ward), Faceted by Hospital}
\usage{
plot_infection_type_by_location(
  data,
  patient_col = "PatientInformation_id",
  center_col = "center_name",
  location_col = "location",
  infection_col = "infection_type",
  location_levels = c("ICU", "Ward"),
  infection_levels = c("HAI", "CAI"),
  colours = NULL,
  ncol = 5,
  base_size = 12,
  show_counts = TRUE,
  title = NULL
)
}
\arguments{
\item{data}{Data frame. Long-format AMR / cleaned stewardship data.}

\item{patient_col}{Character. Patient ID column.
Default \code{"PatientInformation_id"}.}

\item{center_col}{Character. Centre/facility column.
Default \code{"center_name"}.}

\item{location_col}{Character. Location column. Default \code{"location"}.}

\item{infection_col}{Character. Infection-type column.
Default \code{"infection_type"}.}

\item{location_levels}{Character vector. Location values to keep (become the
x-axis bars). Default \code{c("ICU", "Ward")}.}

\item{infection_levels}{Character vector. Infection-type values to keep
(become the stacked fills). Default \code{c("HAI", "CAI")}.}

\item{colours}{Named character vector of fill colours keyed by
\code{infection_levels}. Auto-generated if \code{NULL}.}

\item{ncol}{Integer. Number of facet columns. Default \code{5}.}

\item{base_size}{Numeric. Base font size. Default \code{12}.}

\item{show_counts}{Logical. Print unique-patient counts inside each
segment. Default \code{TRUE}.}

\item{title}{Character. Custom title. Auto-generated if \code{NULL}.}
}
\value{
A \code{ggplot} object (one panel per centre).
}
\description{
100%-stacked bar chart showing, for each hospital, the share of infection
types (e.g. HAI vs CAI) among patients in each location (e.g. ICU vs Ward).
Each bar is normalised \emph{within} its location, so the two bars in a
panel directly compare \eqn{P(\text{infection type} \mid \text{location})}
-- i.e. whether infection type is associated with location, per hospital.
}
\details{
\strong{Deduplication:} unique patients per centre x location x infection
type. Only the requested \code{location_levels} and \code{infection_levels}
are kept (other/blank/compound values are dropped).
}
