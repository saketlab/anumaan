% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_facility_type.R
\name{prep_assign_facility_type}
\alias{prep_assign_facility_type}
\title{Assign a facility type / sector column from a user-supplied mapping}
\usage{
prep_assign_facility_type(
  data,
  facility_col = "center_name",
  mapping,
  new_col = "facility_type",
  default = NA_character_,
  case_insensitive = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{facility_col}{Name of the column holding facility names.
Default \code{"center_name"}.}

\item{mapping}{A named character vector where names are facility values and
values are the type/sector to assign, e.g.
\code{c(AFMC = "Government", SGRH = "Private")}.}

\item{new_col}{Name of the column to create. Default \code{"facility_type"}.}

\item{default}{Value assigned to facilities not found in \code{mapping}.
Default \code{NA_character_}.}

\item{case_insensitive}{Logical. If \code{TRUE} (default), matching ignores
case and surrounding whitespace, so \code{"AFMC"}, \code{"afmc"} and
\code{" AFMC "} all match.}
}
\value{
\code{data} with \code{new_col} added.
}
\description{
Adds a column that labels each row's facility with a category defined by the
caller (for example "Government" vs "Private", "Urban" vs "Rural", or a
region). The function is deliberately generic: it contains no hardcoded
facility names, so any dataset can be classified by passing an appropriate
\code{mapping}.
}
\examples{
df <- data.frame(center_name = c("AFMC", "SGRH", "Unknown"))
m  <- c(AFMC = "Government", SGRH = "Private")
prep_assign_facility_type(df, mapping = m)

}
