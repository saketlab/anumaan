% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_diagnosis.R
\name{prep_map_icd_to_syndrome}
\alias{prep_map_icd_to_syndrome}
\title{Map ICD Codes to Infectious Syndromes}
\usage{
prep_map_icd_to_syndrome(
  data,
  icd_col = "icd_code",
  icd_to_syndrome_ref,
  ref_icd_col = "icd_code",
  ref_syndrome_col = "infectious_syndrome",
  output_col = "syndrome",
  unmatched_label = NA_character_
)
}
\arguments{
\item{data}{Data frame (output of \code{prep_map_diagnosis_to_icd()} or any
data frame with an ICD column).}

\item{icd_col}{Character. Column in \code{data} holding ICD codes or
descriptions to match on. Default \code{"icd_code"}.}

\item{icd_to_syndrome_ref}{Data frame. Reference table with at minimum
\code{icd_col} values and a syndrome column.}

\item{ref_icd_col}{Character. The ICD column name in \code{icd_to_syndrome_ref}.
Default \code{"icd_code"}.}

\item{ref_syndrome_col}{Character. The syndrome column name in the reference.
Default \code{"infectious_syndrome"}.}

\item{output_col}{Character. Name for the syndrome column added to \code{data}.
Default \code{"syndrome"}.}

\item{unmatched_label}{Character. Label for rows with no syndrome match.
Default \code{NA_character_}.}
}
\value{
Data frame with \code{output_col} added.
}
\description{
Joins an ICD-to-syndrome reference table onto the data. The reference must
have at minimum two columns: an ICD code/description column and a syndrome
column. Typically this is a manually curated mapping CSV.
}
