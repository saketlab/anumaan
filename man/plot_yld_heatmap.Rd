% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_daly_plots.R
\name{plot_yld_heatmap}
\alias{plot_yld_heatmap}
\title{Heatmap of YLD per 1 000 Admissions by Organism Group}
\usage{
plot_yld_heatmap(
  data,
  n_admissions = NULL,
  pathogen_col = "pathogen",
  assoc_col = "YLD_associated",
  attr_col = "YLD_attributable",
  group_col = NULL,
  syndrome_col = NULL,
  syndrome_name = NULL,
  show_values = TRUE,
  value_digits = 2,
  palette = "Blues",
  coord_fixed = TRUE,
  base_size = 14,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{data}{Data frame. Must contain the columns named by
\code{pathogen_col}, \code{assoc_col}, and \code{attr_col}.}

\item{n_admissions}{Numeric or \code{NULL}. Total admissions used as the
denominator for per-1 000 normalisation. If \code{NULL} (default),
values are plotted as supplied (assumed pre-normalised).}

\item{pathogen_col}{Character. Column containing pathogen names.
Default \code{"pathogen"}.}

\item{assoc_col}{Character. Column containing YLD associated values.
Default \code{"YLD_associated"}.}

\item{attr_col}{Character. Column containing YLD attributable values.
Default \code{"YLD_attributable"}.}

\item{group_col}{Character or \code{NULL}. Column containing organism
group labels (e.g. \code{"org_group"}). If \code{NULL} (default) or not
present in \code{data}, pathogen names are used on the y-axis.}

\item{syndrome_col}{Character or \code{NULL}. Syndrome filter column.
Default \code{NULL}.}

\item{syndrome_name}{Character or \code{NULL}. Syndrome value to retain.
Requires \code{syndrome_col}. Default \code{NULL}.}

\item{show_values}{Logical. Print rounded values inside each cell.
Default \code{TRUE}.}

\item{value_digits}{Integer. Decimal places for cell labels. Default 2.}

\item{palette}{Character. RColorBrewer palette for the fill scale.
Default \code{"Blues"}.}

\item{coord_fixed}{Logical. Whether to apply \code{ggplot2::coord_fixed()}
to produce square tiles. Default \code{TRUE}.}

\item{base_size}{Numeric. Base font size. Default 14.}

\item{title}{Character. Custom plot title. Auto-generated if
\code{NULL}.}

\item{subtitle}{Character. Custom subtitle. Auto-generated if
\code{NULL}.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a two-column tile heatmap where the x-axis shows YLD Associated
and YLD Attributable, the y-axis shows organism groups (or individual
pathogens), and each cell's fill reflects the YLD burden per 1 000
admissions.
}
\details{
The input \code{data} should contain one row per pathogen with
\code{YLD_associated} and \code{YLD_attributable} columns \u2014 typically
the output of \code{daly_calc_yld_attributable()} (which augments the
data frame with both columns). If an organism-group column has been
pre-joined (e.g. \code{org_group}), supply its name via \code{group_col}
to aggregate cells to the group level.
}
