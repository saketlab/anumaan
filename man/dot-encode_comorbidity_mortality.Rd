% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden.R, R/yll.R
\name{.encode_comorbidity_mortality}
\alias{.encode_comorbidity_mortality}
\title{Encode Comorbidity Column for Mortality Model}
\usage{
.encode_comorbidity_mortality(
  data,
  comorbidity_col = "comorbidities",
  patient_id_col = "PatientInformation_id"
)

.encode_comorbidity_mortality(
  data,
  comorbidity_col = "comorbidities",
  patient_id_col = "PatientInformation_id"
)
}
\arguments{
\item{data}{Patient-level data frame.}

\item{comorbidity_col}{Character. Default \code{"comorbidities"}.}

\item{patient_id_col}{Character. Default \code{"PatientInformation_id"}.}
}
\value{
\code{data} with column \code{comorbidity_encoded} added.
  Attribute \code{"comorbidity_encoding"} records the strategy used:
  \code{"numeric"}, \code{"binary"}, \code{"ordinal"}, or \code{"absent"}.

\code{data} with \code{comorbidity_encoded} added. Attribute
  \code{"comorbidity_encoding"} records the strategy used.
}
\description{
Standardises a free-text or numeric comorbidity column to a consistent
coding for use as a covariate in \code{fit_mortality_rr_logistic()}.

Standardises a comorbidity column to numeric, binary (0/1), or ordinal
factor encoding for use in \code{fit_mortality_rr_logistic()}.
}
\details{
Three encoding strategies are applied in order:
\enumerate{
  \item \strong{Numeric} (Charlson / Elixhauser index already present):
        used as-is.
  \item \strong{Binary text} (\code{"present"} / \code{"none"}, etc.):
        recoded to integer 0 / 1.
  \item \strong{Ordinal text} (\code{"none"} / \code{"mild"} /
        \code{"moderate"} / \code{"severe"}): recoded to an ordered factor.
}
Missing / unknown values are set to \code{NA} in all cases.
}
\keyword{internal}
